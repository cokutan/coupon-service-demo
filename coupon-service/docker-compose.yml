version: "3.9"

services:

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  app:
    image: coupon-service
    depends_on:
      - redis
    environment:
      SPRING_REDIS_HOST: redis
      SPRING_REDIS_PORT: 6379
    ports:
      - "8080"
